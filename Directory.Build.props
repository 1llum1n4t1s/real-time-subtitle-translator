<Project>
  <PropertyGroup>
    <Platforms>x64</Platforms>
    <PlatformTarget>x64</PlatformTarget>
    <SupportedOSPlatformVersion>10.0.20348.0</SupportedOSPlatformVersion>
    <TargetFramework>net10.0-windows10.0.20348.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <Version>1.0.52</Version>
    <!-- ReadyToRun: AOT 互換性に依存せず JIT コンパイル時間を削減（起動時間 30-50% 短縮見込み） -->
    <PublishReadyToRun>true</PublishReadyToRun>
    <!--
      NuGet 依存のサプライチェーン保護: lockfile (packages.lock.json) を生成・検証する。
      これにより「リパッケージ攻撃」(既存バージョンの差し替え) を検知可能になる。
      本体 3 プロジェクト (Core, UI, Tests) すべてが lockfile 対象。
    -->
    <RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>
    <!--
      CI ビルド時のみ lockfile を厳格モードで検証する（ローカル開発では新規パッケージ追加を許可）。
      /rere 第2R #A2-006-R2 (v1.0.29 候補): `$(ContinuousIntegrationBuild)` は dotnet SDK が
      自動設定しない場合があるため、 主要 CI 環境変数 (GITHUB_ACTIONS / TF_BUILD / CI) を明示的に
      見て ContinuousIntegrationBuild を有効化する。 これで lockfile (packages.lock.json) の
      contentHash 検証が「CI で確実に発火する」を構造的に保証 (lockfile drift 防御の死活)。
      MSBuild の Condition は同 PropertyGroup 内で順次評価されるので、 ContinuousIntegrationBuild を先に立ててから RestoreLockedMode を判定。
    -->
    <ContinuousIntegrationBuild Condition="'$(GITHUB_ACTIONS)' == 'true' or '$(TF_BUILD)' == 'true' or '$(CI)' == 'true'">true</ContinuousIntegrationBuild>
    <RestoreLockedMode Condition="'$(ContinuousIntegrationBuild)' == 'true'">true</RestoreLockedMode>
    <!-- bin/obj を Platform/TargetFramework を含めず 1 か所に統一（単一 TFM のため簡潔化） -->
    <BaseOutputPath>$(MSBuildProjectDirectory)\bin\</BaseOutputPath>
    <OutputPath>$(BaseOutputPath)$(Configuration)\</OutputPath>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    <AppendRuntimeIdentifierToOutputPath>false</AppendRuntimeIdentifierToOutputPath>
    <BaseIntermediateOutputPath>$(MSBuildProjectDirectory)\obj\</BaseIntermediateOutputPath>
    <IntermediateOutputPath>$(BaseIntermediateOutputPath)$(Configuration)\</IntermediateOutputPath>
    <MSBuildProjectExtensionsPath>$(IntermediateOutputPath)</MSBuildProjectExtensionsPath>
    <!-- 製作者情報。Company は exe のプロパティに載るので屋号で統一する。 -->
    <Authors>Kagayoi</Authors>
    <Company>Kagayoi</Company>
    <Copyright>Copyright © 2026 Kagayoi</Copyright>
    <!-- 警告をエラー扱いにして品質ゲートを他リポジトリと揃える。 -->
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <!-- IDE コードスタイル分析をビルド時にも強制する。IDE0005 はドキュメント生成が必須なため対象外。 -->
    <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
  </PropertyGroup>
</Project>
